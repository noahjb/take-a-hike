describe('dummy test', () => {
    it('should pass when results match', () => {
        expect(true).toBe(true);
    });

    it('should fail when results differ', () => {
        expect(true).toBe(false);
    });
});
